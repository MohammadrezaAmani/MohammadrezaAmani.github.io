import React from "react";
import { resumeType } from "../../types";

function simpleTheme({
  personal,
  education,
  experiences,
  projects,
  courses,
  publications,
}: resumeType) {
  return <div>simpleTheme</div>;
}

export default simpleTheme;
